% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recursive.R
\name{split_episodes}
\alias{split_episodes}
\title{Split spells at cutpoints for piecewise-constant duration dependence}
\usage{
split_episodes(data, event, exposure, breaks)
}
\arguments{
\item{data}{spell-level data.frame with a 0/1 event indicator.}

\item{event}{name of the 0/1 event column.}

\item{exposure}{name of the spell-duration column (> 0).}

\item{breaks}{increasing positive cutpoints, e.g. `c(3, 12)`.}
}
\value{
data.frame with one row per occupied segment: original columns,
  exposure and event replaced segment-wise, plus `interval` (factor)
  and `.t0` (segment entry time).
}
\description{
Episode splitting: each spell is divided at `breaks`, producing one row per
occupied segment with adjusted exposure, the (0/1) event assigned to the
final occupied segment, and an `interval` factor to include among the
hazard covariates. With enough cutpoints, the piecewise-constant
exponential approximates arbitrary duration dependence (Bartus 2017,
p. 459); for monotone hazards, `family = "weibull"` in the recursive
fitters is the parametric alternative.
}
\examples{
d <- data.frame(id = 1:4, death = c(0, 1, 0, 1),
                months = c(2, 5, 14, 1))
split_episodes(d, event = "death", exposure = "months", breaks = c(3, 12))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiproc.R
\name{stack_processes}
\alias{stack_processes}
\title{Stack several hazard processes into long format}
\usage{
stack_processes(data, id, events, exposure)
}
\arguments{
\item{data}{spell-level data.frame (one row per spell).}

\item{id}{name of the person identifier column.}

\item{events}{named character vector: process name -> event indicator column
(0/1, NA = spell not at risk for that process).}

\item{exposure}{name of the spell-duration column (>0).}
}
\value{
long data.frame with columns `process`, `.event`, `.exposure`,
  plus `id` and all covariates.
}
\description{
Each process is a piecewise-constant exponential hazard observed on spell
rows. Rows where the process's event indicator is missing are dropped for
that process. The result duplicates spell rows once per applicable process
and adds a `process` factor, an `.event` outcome, and a `.exposure` column.
}
